/**
 * This package contains a single top-level class {@link com.eddy.docker.DockerSandbox} and a package api. The only class
 * that this library intends for clients to interact with is the DockerSandbox class which uses the api package to access
 * Docker and return result objects. The only classes from the api package that should be instantiated, should be in the
 * context of those classes required by the DockerSandbox class
 */
package com.eddy.docker;