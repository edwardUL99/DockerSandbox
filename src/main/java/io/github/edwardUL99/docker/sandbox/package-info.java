/**
 * This package contains a single top-level interface {@link io.github.edwardUL99.docker.sandbox.api.DockerSandbox} inside package api. The only interface
 * that this library intends for clients to interact with is the DockerSandbox interface which uses the api package to access
 * Docker and return result objects. The only classes from the api package that should be instantiated, should be in the
 * context of those classes required by the DockerSandbox interface
 */
package io.github.edwardUL99.docker.sandbox;